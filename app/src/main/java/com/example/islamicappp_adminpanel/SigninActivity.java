package com.example.islamicappp_adminpanel;import androidx.appcompat.app.AppCompatActivity;import android.content.Intent;import android.os.Bundle;import android.text.TextUtils;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.Toast;public class SigninActivity extends AppCompatActivity implements View.OnClickListener {    private EditText name, pass;    private String Snme, Spas;    private Button signup_button;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_signin);        name = findViewById(R.id.name);        pass = findViewById(R.id.pass);        signup_button = findViewById(R.id.signup_button);        signup_button.setOnClickListener(this);        findViewById(R.id.back).setOnClickListener(this);    }    @Override    public void onClick(View v) {        switch (v.getId()) {            case R.id.back:                finish();                break;            case R.id.signup_button:                Snme=name.getText().toString().trim();                Spas=pass.getText().toString().trim();                if(TextUtils.isEmpty(Snme)){                    name.setError("Name is Required");                    name.setFocusable(true);                    Snme="";                }                else       if(TextUtils.isEmpty(Spas)){                    pass.setError("Password is Required");                    pass.setFocusable(true);                    Spas="";                }else if (Snme.equals("Admin") && Spas.equals("Admin")){// on success                    startActivity(new Intent( this.getApplicationContext(), MainActivity.class).addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP|Intent.FLAG_ACTIVITY_CLEAR_TASK));                    getSharedPreferences("Login",MODE_PRIVATE).edit().putBoolean("chk",true).apply();                    //do all athe processing here good to go                }                else {                    pass.setError("Wrong password or user name");                    pass.setFocusable(true);                }                break;            case R.id.reset:                Toast.makeText(this, "Feature not available", Toast.LENGTH_SHORT).show();                break;        }    }}